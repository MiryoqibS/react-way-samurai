# 🎯 Тема 10 - Что такое API ?

> 💡 В этой теме Димыч объяснил фундамент: как общается браузер с сервером, что такое API, зачем нужны ключи, как работать с Network, и как вручную отправлять запросы через fetch().

---

## 📘 Что такое API

- API — это «мост» между твоим приложением и сервером.
- Фронтенд → отправляет запрос
- Бэкенд → отвечает данными
- Всё общение идёт через эндпоинты (URL)
- Примеры эндпоинтов:
  - GET https://api.site.com/users
  - POST https://api.site.com/login
  - DELETE https://api.site.com/notes/42

---

## 📘 Что такое API-KEY

- API-KEY — это секретный ключ, который даёт доступ к API.
- Как пароль используется в запросах
- Обычно хранится на сервере или в .env

Пример:

```javascript
fetch("https://musicfun.it-incubator.app/api/playlists/tracks", {
  headers: {
    "API-KEY": "123456789",
  },
});
```

---

## 📘 Swagger — визуальная документация API

> Swagger — это сайт, где описаны все методы бэкенда.

Ты можешь:

- смотреть эндпоинты
- вводить данные
- отправлять запросы прямо с UI
- видеть ответы сервера
- Это как «паспорт API».

---

## 📘 Как делать запросы (JavaScript + fetch)

### 🔥 Пример GET запроса:

```javascript
fetch("https://musicfun.it-incubator.app/api/playlists/tracks", {
  headers: {
    "API-KEY": "123456789",
  },
})
  .then((res) => res.json())
  .then((data) => console.log(data));
```

### 🔥 Пример POST запроса:

```javascript
fetch(
  "https://musicfun.it-incubator.app/api/playlists/tracks/{trackId}/likes",
  {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "API-KEY": "123456789",
    },
  }
)
  .then((res) => res.json())
  .then((data) => console.log(data));
```

---

## 📘 Анализ Network в браузере

Через вкладку Network ты учишься:

- смотреть все запросы
- видеть статус (200, 400, 404, 500)
- смотреть отправленные данные
- смотреть ответ сервера
- дебашить ошибки
- проверять body, headers, параметры

### Почему это важно?

> Потому что разработчик должен понимать, что именно уходит на сервер и что приходит обратно.

---

## 📘 Главное из темы

- API — это способ задать правила общения между фронтом и бэком.
- Каждый запрос имеет:
  - метод (GET, POST, PUT, DELETE)
  - URL
  - headers
  - body (не всегда)
- fetch() — базовый клиент для общения с сервером.
- API-KEY защищает API от доступа посторонних.
- Swagger — инструкция ко всем запросам.
- Network — инструмент самурая для поиска и анализа проблем.

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
