# 🎯 Тема 4 - React-приложение с нуля

> 💡 В этой теме мы разобрали, как фронтендер создаёт современное React-приложение с нуля: установка Vite, настройка TypeScript, структура проекта, компонентный подход и разделение данных от логики.

---

## 📘 Создание React-проекта через Vite

> 💡 Vite — это быстрый инструмент для разработки, который помогает мгновенно запускать и собирать современные frontend-проекты.

- Мы создали новый проект:
- выбрали шаблон react-ts
- установили зависимости (node_modules)
- запустили локальный сервер разработки
- После этого появился чистый React-проект с TypeScript.

---

## 📘 Основные файлы проекта

### ⚙️ `main.tsx`

> 💡 Точка входа, где React «встраивается» в DOM.

Здесь создаётся корневой React-объект и рендерится <App />:

```javascript
const rootEl = document.getElementById("root");
const reactRoot = createRoot(rootEl!);
reactRoot.render(<App />);
```

### ⚙️ `App.tsx`

> 💡 Главный компонент приложения, который отвечает за отображение данных.

Внутри мы храним массив треков:

```javascript
const tracks = [
  { id: 0, title: "Musicfun soundtrack", url: "..." },
  { id: 1, title: "Musicfun soundtrack instrumental", url: "..." },
];
```

И отображаем их через .map():

```javascript
<ul>
  {tracks.map((track) => (
    <li key={track.id}>
      <div>{track.title}</div>
      <audio src={track.url} controls />
    </li>
  ))}
</ul>
```

---

## 📘 Что такое компонент .tsx

> 💡 .tsx — это файл компонента React, который использует JSX и TypeScript одновременно.

### Благодаря TS:

- появляются типы
- IDE подсказывает ошибки
- структура становится предсказуемой и стабильной

---

## 📘 Разделение данных и логики

> 💡 Важная мысль темы: данные не должны смешиваться с отображением.

Мы вынесли массив треков отдельно, а JSX использовали только для рендера.
Это делает код удобнее, понятнее и масштабируемее.

---

## 📘 Что мы узнали на этой теме

### ⚙️ Основные навыки:

- как создать React-проект с Vite
- как работает структура src/
- что такое точка входа (main.tsx)
- что делает главный компонент (App.tsx)
- как работает рендер массива элементов
- почему важна типизация в реактовских компонентах
- зачем разделять данные от верстки

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
