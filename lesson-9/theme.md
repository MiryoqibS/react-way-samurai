# 🎯 Тема 9 - Как работает хук useState()

> 💡 В этой теме Димыч объяснил, что такое состояние компонента, почему React не меняет переменные напрямую и зачем нужен хук useState().

useState() — это способ «заставить React запомнить» значение между рендерами и обновлять интерфейс, когда это значение меняется.

- Когда вызываем setter → React запускает перерендер.
- UI всегда «следует» за состоянием.
- Это фундамент интерактивных компонентов.

---

## 📘 Что такое `useState()`

- Это функция, которая создаёт состояние внутри компонента.
- Возвращает массив из двух элементов:
  - текущее значение
  - функция обновления этого значения

Пример:

```javascript
const [selectedTrackId, setSelectedTrackId] = useState(null);
```

- `selectedTrackId` — текущее состояние
- `setSelectedTrackId` — функция, которая вызывает обновление UI

---

## 📘 Главная идея темы

### 🧠 Состояние управляет интерфейсом

React работает по принципу:

- «Сначала изменяем состояние → React рендерит новый UI».
- Кликаем по треку → меняем состояние
- Состояние меняется → выделяется нужный элемент
- Никаких ручных изменений DOM

---

## 📘 Пример из урока (Musicfun Player)

### 🔥 Выбор трека

```javascript
<div onClick={() => setSelectedTrackId(track.id)}>{track.title}</div>
```

> 💡 Очень важно передавать функцию, а не результат вызова.

> Поэтому пишем () => ..., а не setSelectedTrackId(track.id) напрямую.

### 🔥 Сброс выбора

```javascript
<button onClick={() => setSelectedTrackId(null)}>Reset selection</button>
```

> 💡 Очень важно: после вызова setter React полностью пересобирает интерфейс.

---

## 📘 Что забираем из темы

### 🔥 Основные знания:

- `useState()` делает компонент интерактивным.
- Состояние нельзя менять напрямую — только через setter.
- Изменение состояния вызывает перерендер.
- React сам обновляет только нужные части DOM.
- onClick — это способ привязать действие к состоянию.
- Состояние управляет стилями, выбранными элементами, логикой UI.

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
