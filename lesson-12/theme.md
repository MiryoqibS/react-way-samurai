# 🎯 Тема 12 - Методы массива filter и find, скорость алгоритма O(n)

> 💡 В этой теме Димыч разобрал два важных метода массива — filter и find, показал как они работают изнутри, зачем нужен predicate, и впервые закинул удочку в мир алгоритмов и скорости работы — O(n).

---

## 📘 Метод `filter()`

filter() — это способ создать новый массив, отфильтровав элементы по определённому условию.

Главные моменты:

- filter возвращает новый массив
- но объекты внутри — те же самые ссылки, что и в оригинале
- если изменить объект в одном массиве → он изменится и в другом
- каждая проверка элемента — это вызов predicate-функции

> 📌 Predicate — это функция, которая должна вернуть true или false.

Мой код:

```javascript
const adults = persons.filter(predicateForAdults);
const children = persons.filter(predicateForChildren);

function predicateForAdults(person, index, array) {
  return person.age >= 18;
}

function predicateForChildren(person, index, array) {
  return person.age < 18;
}
```

---

## 📘 Метод `find()`

find() возвращает первый элемент, который подходит под условие.
Если ничего не нашёл — возвращает undefined.

Пример:

```javascript
const adult = persons.find((person) => person.age >= 18);
```

---

## 📘 Закинутая удочка — скорость алгоритма O(n)

Димыч объяснил важную вещь:

- и filter, и find перебирают элементы массива от начала до конца
- используется курсор-указатель, который идёт по каждому элементу
- время работы → O(n) (чем больше элементов, тем дольше)

Это первый шаг к пониманию алгоритмов.
Дальше будет сложнее, но фундамент заложен уже здесь.

---

## 📘 Как это выглядит в моей реализации

Вот код, который я писал по теме:

```javascript
const persons = [
  { name: "dmitry", age: 37 },
  { name: "john", age: 17 },
  { name: "rita", age: 18 },
];

// filter
const adults = persons.filter(predicateForAdults);
const children = persons.filter(predicateForChildren);

function predicateForAdults(person, index, array) {
  return person.age >= 18;
}

function predicateForChildren(person, index, array) {
  return person.age < 18;
}

console.log(adults);
console.log(children);

// find
const adult = persons.find((person) => person.age >= 18);

console.log(adult);
```

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
