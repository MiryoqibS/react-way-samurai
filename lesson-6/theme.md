# 🎯 Тема 6 - Модули, import, export，default export

> 💡 В этой теме Димыч объяснил, как устроены модули в JavaScript, почему они стали стандартом, и как правильно использовать import/export, чтобы проект оставался чистым, читаемым и масштабируемым.

---

## 📘 Почему вообще нужны модули

> 💡 Модули позволяют разделять код на логические части — каждый файл отвечает за свою задачу. Это фундамент современной фронтенд-разработки.

Основные причины:

- Наводят порядок в проекте
- Избегают конфликтов имён
- Позволяют переиспользовать код
- Делают архитектуру понятной
- Упрощают поддержку приложения
- JavaScript раньше был «одним файлом на всё», но сегодня — всё через модули. Это путь самурая: порядок вместо хаоса.

---

## 📘 Типы экспорта в JavaScript

### 🔹 1. Именованный экспорт (`named export`)

> 💡 Можно экспортировать сколько угодно сущностей из одного файла.

```javascript
export function runPlayer() {
  console.log("player started");
}

export function run() {
  console.log("run player");
}
```

Импорт:

```javascript
import { runPlayer, run } from "./player/player.js";
```

### 🔹 2. Переименование при импорте

> 💡 Нужно, когда названия могут конфликтовать.

```javascript
import { run as analyticsRun } from "../player/player.js";
```

### 🔹 3. default export

> 💡 В файле может быть только один default.
> Используется для главной функции или основного объекта модуля.

```javascript
export default function start() {
  console.log("start app");
}
```

Импорт:

```javascript
import start from "./app.js";
```

### 🔹 4. Импорт всего (import \*)

> 💡 Собирает все экспорты файла в объект.

```javascript
import \* as player from "./player.js";

player.run();
player.runPlayer();
```

---

## 📘 Как подключить модуль в HTML

> 💡 Чтобы браузер понимал import/export, нужен type="module".

```javascript
<script type="module" src="./main.js"></script>
```

Без этого браузер вообще не поймёт, что такое import/export.

---

## 📘 Код, который разбирали на уроке

`📁 main.js`

```javascript
import { runPlayer, run } from "./player/player.js";
import "./plugin.js";

runPlayer();
run();
```

`📁 player/player.js`

```javascript
import { run as analyticsRun } from "../player/player.js";

export function runPlayer() {
  console.log("player started");
  analyticsRun();
}

export function run() {
  console.log("run player");
}
```

`📁 plugin.js`

```javascript
function run() {
  console.log("run player");
}

run();
console.log("plugin file initialized");
```

`📁 index.html`

```javascript
<script type="module" src="./main.js"></script>
```

---

## 📘 Главные мысли самурая из этой темы

### ⚙️ Главное, что мы забираем:

- Модуль — это отдельный, независимый кусок логики
- Импорт можно делать по-разному: {}, \*, default, alias
- Каждый файл выполняется один раз
- Модули автоматически работают в строгом режиме
- Архитектура проекта строится через модули
- Без модулей невозможен ни React, ни Vite, ни современный фронтенд. Это фундамент, на котором держится всё.

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
