# 🎯 Тема 7 - Что такое JSX?

> 💡 В этой теме Димыч объяснил, что JSX — это не HTML, а синтаксический сахар над JavaScript. Он не работает с тегами браузера напрямую — он создаёт структуру React-элементов, которые потом превращаются в DOM.

---

## 📘 Почему JSX — это не HTML

> 💡 Внешне JSX очень похож на HTML, но работает совершенно по-другому.

### ⚙️ Главное отличие:

- HTML → работает с реальными DOM-элементами
- JSX → создаёт виртуальные React-элементы
- JSX — это просто JavaScript, который пишется в виде разметки.

Пример:

```javascript
const el = <h1>Hello</h1>;
```

На самом деле превращается в:

```javascript
React.createElement("h1", null, "Hello");
```

---

## 📘 Почему JSX строже, чем HTML

> 💡 В HTML можно писать неаккуратно: забыть закрывающий тег, нарушить структуру — браузер сам дорисует.

Но в JSX так не работает. Самые важные различия:

- все теги должны быть закрыты
- синтаксис ближе к JavaScript, чем к HTML
- нельзя использовать ключевые слова, пересекающиеся с JS
- JSX всегда должен быть валидным JavaScript-выражением

---

## 📘 Отличие HTML-атрибутов и JSX-атрибутов

> 💡 В JSX многие атрибуты называются по-другому, потому что это JS, а не HTML.

Самые важные:

| HTML     | JSX       |
| -------- | --------- |
| class    | className |
| for      | htmlFor   |
| tabindex | tabIndex  |
| onclick  | onClick   |

React использует `camelCase`, потому что это стандарт JS.

---

## 📘 Почему `<input>` работает в HTML, но не работает в React

> 💡 В HTML `<input>` можно не закрывать, браузер сам разберётся.
> В JSX — нельзя. JSX требует строгого синтаксиса.

Пример:

HTML:

```html
<input />
```

JSX:

```javascript
<input /> // обязательное самозакрытие
```

> Если написать `<input>` в JSX без /, React выдаст ошибку — потому что он не может корректно преобразовать тег в JavaScript-структуру.

---

## 📘 Что мы забираем из этой темы

### ⚙️ Основные знания:

- JSX ≠ HTML
- JSX — это JS, который выглядит как разметка
- каждый элемент должен быть валидным выражением
- все теги самозакрывающиеся должны оканчиваться на /
- атрибуты пишутся в camelCase
- JSX создаёт виртуальный DOM, а не реальный DOM напрямую
- Понимание JSX — это фундамент React.
- С него начинается компонентный подход и вся магия декларативного UI.

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
