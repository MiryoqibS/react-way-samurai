# 🎯 Тема 2 - Как работает фронтенд

> 💡 В этой теме мы разобрали, что происходит "под капотом" фронтенда: как браузер обрабатывает запросы, что такое домены, как работает рендеринг и почему важно разделять данные, логику и отображение.

---

## 📘 Как работает фронтенд под капотом

> 💡 Браузер получает HTML-файл, загружает скрипты, строит DOM-дерево и обновляет интерфейс при изменении данных. Мы подробно прошли путь запроса: DNS → домен → сервер → ответ → браузер.

### ⚙️ Основные моменты:

- Как работает браузер и DOM
- Что такое домены и DNS
- Как происходит загрузка и выполнение JS
- Почему важно понимать архитектуру приложения

---

## 📘 Архитектурный принцип

> 💡 Главная мысль урока — разделять данные, логику рендера и отображение.
> Даже простой проект становится чище, если каждый слой отвечает только за своё.

---

## 📘 Практическое задание

> 💡 Мы создали небольшой проект: музыкальный плеер, где данные хранятся отдельно, а рендеринг строится через работу с DOM.

### ⚙️ Структура проекта:

- HTML-каркас с корневым элементом
- Массив tracks как данные
- Создание DOM-элементов через JavaScript
- Рендер списка треков и плеера

---

## 📘 Код проекта

```javascript
const tracks = [
  {
    title: "Musicfun soundtrack",
    url: "https://musicfun.it-incubator.app/api/samurai-way-soundtrack.mp3",
  },
  {
    title: "Musicfun soundtrack instrumental",
    url: "https://musicfun.it-incubator.app/api/samurai-way-soundtrack-instrumental.mp3",
  },
];

const rootEl = document.getElementById("root");

const headerEl = document.createElement("h1");
headerEl.append("Musinfun Player");
rootEl.append(headerEl);

const tracksEl = document.createElement("ul");
tracks.forEach((track) => {
  const trackEl = document.createElement("li");

  const trackTitleEl = document.createElement("div");
  trackTitleEl.append(track.title);
  trackEl.append(trackTitleEl);

  const trackPlayerEl = document.createElement("audio");
  trackPlayerEl.controls = true;
  trackPlayerEl.src = track.url;

  trackEl.append(trackPlayerEl);
  tracksEl.append(trackEl);
});
rootEl.append(tracksEl);
```

---

## 🙏 Благодарность Димычу (IT-KAMASUTRA)

> 💡 Спасибо тебе, Димыч, за труд, за искренность и за то, что ты не просто учишь React, а даёшь направление — как мыслить, как расти и не бояться нового. Этот курс не про синтаксис, а про путь. И каждый, кто пройдёт его с открытой головой, выйдет не просто разработчиком, а самураем, готовым к реальному миру.
